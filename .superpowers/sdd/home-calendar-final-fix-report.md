# Home calendar final mobile containment fix

## Scope

Fixed the 390px home-calendar regression where the representative long personal label, long candidate label, and the candidate status could escape a fixed-height single-day event chip. The desktop event geometry remains unchanged.

## RED → GREEN

### RED

Command:

```text
npm run test:product-ui:home-browser
```

Exact output:

```text
> test:product-ui:home-browser
> node product-ui/tests/verify-home-browser.js

AssertionError [ERR_ASSERTION]: personal-long-label label escapes its 390px home calendar chip
    at run (/Users/openclaw/projects/kdhc-ai-contest/.worktrees/feature-on-memory-workbench/product-ui/tests/verify-home-browser.js:438:14)
```

The new 390px regression verifies both representative labels, the exact candidate status `후보 · 미확인`, text scroll dimensions, and each child text rectangle staying within its event-chip rectangle.

### GREEN

Command:

```text
npm run test:product-ui:home-browser
```

Exact output:

```text
> test:product-ui:home-browser
> node product-ui/tests/verify-home-browser.js

Home browser contract passed
```

The narrow `max-width:640px` layer now gives single-day events an 86px chip within a 92px calendar lane. The lane uses the existing desktop 60px default, so desktop layout is not changed.

## Capture and visual review

`npm run capture:product-ui:home` passed and regenerated `product-ui/screenshots/home-two-week-context.png`:

```text
Captured desktop home: product-ui/screenshots/home-two-week-context.png (1920/1920)
Captured mobile home: /var/folders/zz/b798sxj96mqd9tbbxbx83g5w0000gn/T/kdhc-home-two-week-mobile.png (390/390)
```

The capture fixture now runs the same 390px containment checks as the browser contract. Desktop image inspection found the calendar-first hierarchy intact, readable toolbar and event chips, the 36px desktop separation preserved, and no visible regression. The capture’s 390px document-overflow check also passed (`390/390`), with horizontal scrolling still contained by the calendar region.

## Validation

All completed with exit code 0:

- `npm run test:product-ui:home-browser`
- `node --check product-ui/app.js`
- `node --check tools/capture-home-two-week-context.js`
- `npm run capture:product-ui:home`
- `npm run test:product-ui:source`
- `npm run test:product-ui:schedule-browser`
- `git diff --check`
- `npm run test:product-ui`

The full `npm run test:product-ui` suite passed, including source/model/state contracts, all browser contracts, fixture/capture/sync checks, and the end-to-end suite.

## Files changed

- `product-ui/app.js` — emits a CSS-variable single-day lane size with the existing 60px desktop fallback.
- `product-ui/style.css` — applies the 390px-relevant mobile 86px chip / 92px lane sizing.
- `product-ui/tests/verify-home-browser.js` — adds the focused 390px label/status containment regression.
- `tools/capture-home-two-week-context.js` — applies the same containment test to the 390px capture fixture.
- `product-ui/screenshots/home-two-week-context.png` — regenerated by the capture command (its final bytes were unchanged from the checked-in artifact).

## Self-review

- The browser test first failed for the actual escaping label, then passed after the mobile-only fix.
- The event model, 14-day window, routes/actions, desktop 36px spacing, mobile 24px spacing, and document-level no-overflow assertions remain covered and pass.
- No pre-existing `.superpowers/sdd/task-2-report.md` or `product-ui/screenshots/showcase-golden.png` changes were staged.

## Concerns

None. The tradeoff is intentionally taller single-day rows at small widths so all visible text is readable rather than clipped; calendar horizontal scrolling remains scoped to the calendar region.

## Commit

`Fix mobile home calendar event containment` (final commit; hash is reported in the task handoff).
